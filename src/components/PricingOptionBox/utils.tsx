type item = {
  text: string;
  checked: boolean;
};

export type PricingOptionType = {
  title: string;
  subtitle: string;
  price: string;
  priceSpan: string;
  list: item[];
};

export const PricingOptionContent = [];
